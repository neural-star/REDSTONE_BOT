[project]
name = "redstone-bot"
version = "0.1.0"
description = "水地サバの補助サーバー(?)用のBOTです"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bandit>=1.9.4",
    "black>=26.3.1",
    "pip-audit>=2.10.0",
    "ruff>=0.15.5",
]
